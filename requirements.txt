# --- App/UI ---
streamlit==1.37.1
python-dotenv==1.0.1

# --- Retrieval & Embeddings ---
sentence-transformers==2.7.0
chromadb==0.5.3
langchain-text-splitters==0.2.2
rank-bm25==0.2.2
nltk==3.9.1
tqdm==4.66.4

# --- LLM Providers (generation & eval) ---
openai==1.37.0
google-generativeai==0.7.2

# --- Web fallback (non-Wikipedia) ---
duckduckgo-search==6.1.3
readability-lxml==0.8.1
trafilatura==1.12.0
beautifulsoup4==4.12.3
lxml==5.2.2
httpx==0.27.0

# --- YAML / misc ---
PyYAML==6.0.2

# --- Optional: PyTorch
# If you need local GPU/CPU embeddings outside Colab, uncomment one of these:
# torch==2.3.1              # Linux/Windows (CPU)
# torch==2.3.1 --index-url https://download.pytorch.org/whl/cu121   # CUDA 12.1
# On Apple Silicon, install the Metal build following PyTorch docs.
